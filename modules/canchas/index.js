const router = require("./canchas.routes");
const viewRouter = require("./canchas.view.routes");

module.exports = {
    basePath: "/canchas",
    viewBasePath: "/canchas",
    router,
    viewRouter,
};
